apiVersion: apps/v1
kind: Deployment
metadata:
  name: votecanary
  labels:
    app: votecanarylabel
spec:
  replicas: 2
  selector:
    matchLabels:
      app: votecanarylabel
  template:
    metadata:
      labels:
        app: votecanarylabel
    spec:
      containers:
      - name: votecanary
        image: celiaoued/vote-app:TAGC
        ports:
        - containerPort: 80
        resources:
          requests:
            cpu: "250m"
          limits:
            cpu: "500m"
        env:
        - name: REDIS
          value: "rediscluster"
        - name: STRESS_SECS
          value: "2"
        - name: REDIS_PWD
          valueFrom:
            secretKeyRef:
              name: redispw
              key: password
